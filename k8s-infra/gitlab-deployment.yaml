apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitlab
  namespace: cc-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitlab
  template:
    metadata:
      labels:
        app: gitlab
    spec:
      containers:
      - name: gitlab
        image: registry.docker.ir/gitlab/gitlab-ce:16.11.2-ce.0
        ports:
        - containerPort: 8929
          name: http
        - containerPort: 22
          name: ssh
        env:
        - name: GITLAB_OMNIBUS_CONFIG
          value: |
            external_url 'http://gitlab.cc-project.svc.cluster.local:8929'
            gitlab_rails['gitlab_shell_ssh_port'] = 30022
---
apiVersion: v1
kind: Service
metadata:
  name: gitlab
  namespace: cc-project
spec:
  type: NodePort
  selector:
    app: gitlab
  ports:
  - port: 8929
    targetPort: 8929
    nodePort: 30880
    name: http
  - port: 22
    targetPort: 22
    nodePort: 30022
    name: ssh